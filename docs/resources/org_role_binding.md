---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "evroc_org_role_binding Resource - evroc"
subcategory: ""
description: |-
  Manages an organization-scoped IAM role binding. A role binding grants one or more roles to a principal (user or service account) across the entire organization. Only one role binding may exist per principal in a given organization.
---

# evroc_org_role_binding (Resource)

Manages an organization-scoped IAM role binding. A role binding grants one or more roles to a principal (user or service account) across the entire organization. Only one role binding may exist per principal in a given organization.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Unique identifier for the role binding. Any name is accepted, but by convention names follow u-{user uuid} for users and sa-{project}-{service account name} for service accounts.
- `principal` (String) Principal FQID (e.g., /iam/users/<uuid> or /iam/projects/<project>/serviceAccounts/<name>).
- `roles` (Block List, Min: 1) Roles granted to the principal at organization scope. (see [below for nested schema](#nestedblock--roles))

### Optional

- `display_name` (String) Optional human-friendly display name for the binding.
- `organization` (String) Organization to create the role binding in. Defaults to the provider organization.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `user_labels` (Map of String) User-defined labels (key/value pairs) for organizing and selecting resources.

### Read-Only

- `created_at` (String) Timestamp when the role binding was created (RFC3339 format).
- `id` (String) The ID of this resource.
- `uid` (String) System-assigned unique identifier (UUID) of the role binding.

<a id="nestedblock--roles"></a>
### Nested Schema for `roles`

Required:

- `role` (String) Role to grant. Can be a short name (e.g., "computeOperator") or full FQID ("/iam/roles/computeOperator").

Optional:

- `resources` (List of String) Optional list of resource FQIDs the role is limited to. Supports wildcards (*) for path segments. Omit to grant the role on all applicable resources.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String)
- `delete` (String)
- `update` (String)
